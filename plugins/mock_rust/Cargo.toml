[package]
name = "mock_rust"
version = "1.0.0"
edition = "2021"
description = "Tree Canopy Density analytical mock plugin compiled in Rust"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
miniz_oxide = "0.8"
